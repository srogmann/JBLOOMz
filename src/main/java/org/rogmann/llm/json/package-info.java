/**
 * JSON-parser to be used in JBLOOM (to get rid of dependencies). It uses a subset of the API of org.json.
 * <p>Use org.json or something else in other projects.</p>
 */
package org.rogmann.llm.json;